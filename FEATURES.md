# 功能清单

本文档对应 `main` 分支当前实现。新增、修改或删除机器人功能时，必须同步更新本文档、机器人内置 `@nao 帮助` 文本和相关测试。

## 使用范围与权限

- 只处理 `NAO_TEST_GROUP_ID` 指定的一个 QQ 群，其他群事件会被忽略。
- `NAO_ADMIN_QQ_IDS` 非空时，只有名单内的 QQ 号可以执行管理命令。
- `NAO_ADMIN_QQ_IDS` 留空时，使用 QQ 群主和群管理员身份判断权限。
- 机器人自己的消息不会进入反诈处罚流程。
- 管理员消息默认跳过自动反诈检测，避免管理通知和演示内容被误判。
- 查询、管理、AI 和表情命令统一使用 `@nao 指令`，旧斜杠命令不再注册。
- 固定问候、自定义关键词回复和 EmojiMix 不要求 `@` 机器人。
- 群内 `@nao 帮助` 只展示常用趣味功能和实验室问答索引；未展示的 AI、定时、管理和反诈命令仍然可用，并继续在本文档中维护。

## AI 问答

- 在群里直接发送 `@nao 你的问题` 即可提问，不需要额外的“问”指令。
- 使用 `DEEPSEEK_MODEL` 指定的 DeepSeek 模型，默认 `deepseek-v4-flash`。
- 每个 QQ 用户有 10 秒 AI 请求冷却时间。
- 提示模型输出适合 QQ 的纯文本。
- 单次 DeepSeek 请求同时返回回答文字、表情场景、对话语境和匹配置信度；场景元数据不会显示在 QQ 中。
- 自动清理标题、粗体、列表、代码围栏、表格、引用、链接等 Markdown 格式。
- 最终回复超过 1500 个字符时会截断并提示。
- DeepSeek 请求失败时返回简短错误提示，不阻塞其他机器人功能。
- 可选场景为：开心、无语、委屈、震惊、吃瓜、摸鱼、加班、鼓励、道歉、晚安、拒绝、阴阳怪气、问候、思考、庆祝、好笑。
- 匹配置信度低于 0.6 时不发表情；严肃、日常和玩笑语境的最高触发概率分别为 10%、45% 和 90%，实际概率再乘以匹配置信度。
- 只从场景标签匹配的私有素材和对应的 NAO 内置贴纸中选择，未标注素材不会进入候选池。
- 每位成员最近使用的 3 张素材会在存在其他候选时被降权；这不是时间冷却，候选只有一张时可以连续使用。
- 图片资源缺失、目录无匹配素材、随机未命中或图片发送失败时只发送文字。
- 自动贴纸只用于 AI 成功回答，不用于反诈、群管理、管理员提示、固定回复或关键词回复。

## 小火人主动接梗

- 普通群聊不需要 `@nao`；DeepSeek 会结合最近 6 条候选消息，判断当前消息是否有明显的梗、反差、调侃或自然接话点。
- 只有模型明确选择直接回复且置信度不低于 `0.75` 时才发送一到三句、约 20 到 120 个汉字的回复；普通陈述、技术讨论、严肃求助、争吵、隐私、广告和链接默认保持沉默。
- 能直接理解的内容使用普通 DeepSeek 对话判断；遇到值得接但涉及近期或陌生网络梗的内容时，搜索判断置信度达到 `0.65` 即进入 `deepseek-v4-flash` Responses API 的 `web_search` 联网核实，联网生成结果仍需达到 `0.75` 才会发送，不会为每条群消息联网搜索。
- 每个群最多每 8 秒调用一次主动判断；成功主动回复后进入 45 秒群级冷却，避免连续插话和增加不必要的 DeepSeek 请求。
- 机器人自己的消息、已有固定回复或关键词回复的消息不会进入主动模式，防止自我循环和重复回复。
- 原有 `@nao 你的问题`、命令、定时、游戏和管理功能保持原匹配优先级；明确 `@nao` 时不会进入主动模式。

## 多人复读

- 同一群 30 秒内有 3 名不同成员发送完全相同的普通纯文本时，机器人原样补发一次；同一成员重复发送只计 1 人。
- 文本只去除首尾空白，大小写、标点和内部空格必须一致；链接、回复、提及、图片、机器人消息、命令、固定回复和关键词回复不会参与统计。
- 同一轮只复读一次；该文本连续 30 秒无人发送后才会开始新一轮。状态只保存在内存中，机器人重启后清空。
- 复读触发时会阻止同一条消息继续进入小火人模式，并废弃该群尚未发送的旧主动回复，避免机器人连续发两条。

## 固定回复

| 触发内容 | 行为 |
| --- | --- |
| `@nao 帮助` | 显示机器人内置命令列表 |
| `@nao 状态` | 返回在线状态 |
| `@nao 关于` | 返回机器人介绍 |
| `你好` | 返回固定问候 |

## 自定义关键词回复

- `@nao 关键词 添加 触发词=回复内容`：添加或更新关键词。
- `@nao 关键词 删除 触发词`：删除关键词。
- `@nao 关键词 列表`：查看现有关键词。
- 仅管理员可以维护关键词库。
- 普通消息与触发词完全一致时回复对应内容。
- 最多保存 50 条；触发词最长 30 个字符，回复最长 500 个字符。
- 数据通过临时文件加原子替换写入 `data/keywords.json`。

## 实验室问答

- `data/lab_faq.json` 使用 `{"问题":"答案"}` 格式保存最多 30 条实验室常见问题；文件不超过 64 KiB，问题最长 100 个字符，答案最长 1000 个字符。
- `@nao 帮助` 会在固定菜单后追加当前问题索引；成员发送 `@nao 完整问题` 时直接返回对应答案，不调用 DeepSeek。
- 文件修改后无需重启。JSON 编辑期间格式无效时保留上一份有效内容，修正后自动重新加载。
- 文件路径可通过 `NAO_LAB_FAQ_FILE` 调整；示例见 `examples/lab_faq.example.json`。

## 表情功能

- `@nao 表情包制作`：查看和使用 `nonebot-plugin-memes` 提供的表情模板。
- 具体模板也使用 mention 触发，例如 `@nao 摸 @成员`。
- 支持使用发送者头像、被提及成员头像和文字生成模板表情。
- `😂+🥺` 形式可以显式合成两个 Emoji。
- EmojiMix 自动识别已关闭，避免普通聊天误触发。
- 表情资源和插件数据库保存在 `data/`。
- 月薪喵素材来自[原作者公开下载页](https://www.mfuns.net/article/120254)，仅保存在私有 `data/reaction_packs/monthly_salary_cat/`，不提交到公开仓库；素材缺失或发送失败时 AI 回答会自动回退为纯文字。
- `data/reaction_catalog.json` 是人工标注接口。机器人首次启动及发现新素材时会自动追加空标签条目，不会覆盖已有 `scenes` 或 `enabled` 设置。
- 每个条目包含相对 `file`、场景数组 `scenes` 和启用开关 `enabled`；格式示例见 `examples/reaction_catalog.example.json`。
- 修改标注文件后无需重启，下一次相关 AI 回复会重新读取；JSON 格式错误时忽略私有素材并记录日志，文字回答仍正常发送。

## 新成员欢迎

- 新成员加入指定测试群时，机器人会 `@` 对方并发送欢迎消息。
- 机器人自己加入事件不会触发欢迎消息。

## 定时提醒

以下命令仅限有管理权限的用户使用：

| 命令 | 行为 |
| --- | --- |
| `@nao 定时 YYYY-MM-DD HH:MM 提醒内容` | 创建一次性群提醒 |
| `@nao 定时任务，本周五 09:00 提醒内容` | 明确要求 DeepSeek 通过定时工具解析并创建提醒 |
| `@nao 每天晚上九点提醒我写 donelist` | 管理员在普通对话中自然表达，由 DeepSeek 判断并调用定时工具 |
| `@nao 定时列表` | 按时间查看当前群待发送的提醒 |
| `@nao 取消定时 任务编号` | 取消当前群的指定提醒 |

- 智能定时由 DeepSeek 通过受约束的 `create_reminder` 工具返回下一次时间、内容和重复周期；模型不直接写数据库，也不能执行其他群管理操作。
- 工具只会提供给管理员；普通成员可以继续使用 AI 问答，但不能通过对话创建定时任务。
- 提醒时间固定按北京时间（`Asia/Shanghai`）解释，必须晚于当前时间；支持一次性、每天和每周重复，不写具体时刻时默认 `09:00`。
- 每个群最多保存 50 条待发送提醒，内容最长 500 个字符。
- 列表最多展示前 20 条，长内容会缩略显示。
- 到期检查间隔为 15 秒，机器人会在原群 `@` 创建者并发送提醒内容。
- 任务持久化到 `data/reminders.sqlite3`；机器人重启后仍会继续处理，已经到期的任务会在连接恢复后发送。
- 消息发送失败时任务不会删除，机器人会在后续检查中重试。

## 趣味游戏

- `@nao 今日人品`：查询当天固定的人品指数；还支持 `@nao 本周人品`、`@nao 本月人品` 和 `@nao 平均人品`。
- `@nao 猜成语`：开始汉字 Wordle，开始后直接发送四字词语；发送“提示”或“结束”控制当前游戏。
- `@nao 人生重开`：开始人生重开模拟，按提示直接回复天赋和属性；`@nao 人生重开 随机` 可自动选择。
- `@nao 猜人物`：启动 DeepSeek 二十问猜人物；开始后直接回复“是、否、不知道、可能、可能不是”，发送“退出”或“结束”终止。
- 趣味插件只在配置的测试群响应，启动时必须 `@` 机器人；交互过程不需要重复 `@`。
- 猜人物最多提问 20 次，5 分钟无操作会话失效；每个群成员的游戏互不影响，DeepSeek 请求失败时可重发刚才的答案。
- 每日人品数据通过 LocalStore 持久化到 `data/`，其他游戏状态只保存在内存中，机器人重启后结束。

## 群管理

- 踢出、禁言、撤回等群管理命令已全部停用，机器人不再执行任何踢人、禁言或撤回他人消息的操作。

## 自动反诈检测

自动检测只处罚普通成员，命中后会优先撤回原消息。

### 文字规则

- 高风险诈骗话术：刷单、垫付返现、解冻金、安全账户、共享屏幕转账、代收验证码、保本投资、裸聊等。
- 论文代写广告：论文代写或辅导、盲审修改、答辩修改、包通过、不过退款等组合信号。
- 兼职和收益诱导：线上兼职、临时工、日结、高佣、高回报、无门槛收益等。
- 站外引流：微信、QQ 群、扫码、私聊联系方式等。
- 资金诱导：转账、汇款、定金、充值、银行卡、验证码、贷款等。
- 可疑链接：网址以及引导点击链接的话术。
- 除直接高风险话术外，通常需要同时命中至少两类信号才判定为诈骗广告。

### 重要通知保护

- “重要通知、紧急通知、官方通知、群通知、重要公告”等内容只允许管理员发送。
- 普通成员通过文字、图片或视频冒充通知时会被撤回并记为违规。

### QQ 名片保护

- 识别 QQ 好友名片和群名片对应的 `light_app`、XML 消息。
- 普通成员发送名片时会被撤回并记为违规。

### 图片保护

- 下载图片最大 8 MB，最大 2000 万像素，处理前最长边缩放到 1920 像素。
- 使用 OpenCV 检测二维码。
- 使用本地 RapidOCR 提取图片文字，不需要额外的云端 OCR 服务。
- OCR 文字会继续经过重要通知、内置诈骗规则和自定义违规词黑名单检测。
- 表情贴纸不会进入普通图片扫描流程。

### 视频保护

- 直接发送的视频和以群文件发送的视频均支持反诈检测；群文件仅识别常见视频扩展名，包括 `.mp4`、`.mov`、`.m4v`、`.mkv`、`.webm`、`.avi`、`.flv`、`.wmv`、`.3gp` 和 `.ts`。
- 视频文件最大 48 MB，下载到临时文件后由 OpenCV 读取；群文件通过 Milky 的群文件下载地址接口获取。
- 从 0 秒开始每 5 秒提取一帧，使用本地 RapidOCR 识别画面文字。
- 每一帧先检测二维码，命中后立即停止该视频的后续处理，不再启动该帧 OCR；未命中二维码时再识别文字，并在当前帧命中重要通知、内置诈骗规则或自定义违规词后停止后续抽帧。
- 视频检测完成或失败后会删除临时视频文件，不保留视频内容。

## 违规计数与处罚

- 每次自动反诈命中后，在 `data/moderation.json` 中累计该群成员的违规次数。
- 处罚方式只有撤回消息并 `@` 成员提示，不会踢出或禁言成员。
- `@nao 反诈状态`：查看当前反诈防护状态。
- `@nao 反诈记录 @成员` 或 `@nao 反诈记录 QQ号`：查看累计次数和最近原因。
- `@nao 清除违规 @成员` 或 `@nao 清除违规 QQ号`：清除误判记录。

## 动态违规词黑名单

以下命令仅限管理员使用：

| 命令 | 行为 |
| --- | --- |
| `@nao 添加违规 违规原文` | 从原文提取高风险短语并加入黑名单 |
| 回复违规消息后发送 `@nao 添加违规` | 从被回复消息的文字中提取短语 |
| `@nao 违规词列表` | 查看当前黑名单 |
| `@nao 删除违规词 词条` | 删除指定黑名单词条 |

- DeepSeek 从原文提取候选短语，本地代码负责最终校验。
- 候选词必须真实出现在原文中，最多接收 8 条。
- 拒绝“微信、论文、服务、联系、通知”等单独出现时可能正常的宽泛词。
- 自动去除重复和被更长短语包含的低信息词。
- DeepSeek 失败或提取不完整时，确定性规则仍会补充已知高风险短语和联系方式。
- 匹配时忽略大小写、空白和常见标点，可识别插入空格或符号的简单规避写法。
- 黑名单同时作用于群文字、图片和视频 OCR 结果，数据保存在 `data/fraud_keywords.json`。
- `@nao 添加违规` 只维护黑名单，不会追溯处罚被回复消息的发送者；新规则从后续消息开始生效。

## 数据持久化

| 路径 | 内容 |
| --- | --- |
| `data/keywords.json` | 自定义关键词回复 |
| `data/moderation.json` | 群成员反诈违规次数和最近原因 |
| `data/fraud_keywords.json` | 动态违规词黑名单 |
| `data/reminders.sqlite3` | 待发送的管理员定时提醒 |
| `data/lab_faq.json` | 实验室常见问题和答案 |
| `data/meme_generator/` | 表情包资源 |
| `data/reaction_packs/monthly_salary_cat/` | 私有月薪喵动态表情素材 |
| `data/reaction_catalog.json` | 私有表情的人工场景标签和启用状态 |
| `data/local/`、`data/cache/`、`data/config/` | NoneBot 插件数据和缓存 |
| `lagrange-data/` | QQ 登录状态、Lagrange 配置和二维码 |

这些目录和真实 `.env` 均不会提交到 Git。

部署时必须分别配置 `LAGRANGE_SIGNER_TOKEN` 和 `MILKY_ACCESS_TOKEN`。前者用于 QQ 协议签名，后者只用于 Lagrange.Milky 与 NoneBot 之间的通信，不能混用。

## 当前限制

- 当前只支持一个配置群，不是多群独立配置系统。
- 图片下载或 OCR 失败时只记录错误并跳过该图片，不会仅凭失败处罚成员。
- 视频下载、解码或 OCR 失败时只记录错误并跳过该视频，不会仅凭失败处罚成员；临时视频文件会自动删除。
- 群文件只有常见视频扩展名会进入检测，其他文件类型会忽略。
- 反诈仍是规则和黑名单系统，可能出现漏判或误判；管理员需要通过记录查询、清除记录和删除违规词维护结果。
- 机器人必须拥有足够的 QQ 群权限才能撤回被反诈检测命中的违规消息。
- 回复式 `@nao 添加违规` 当前只读取被回复消息中的文字，不会对图片重新执行学习。
- 私有表情必须先在 `reaction_catalog.json` 中标注场景才会被自动选中；机器人不会猜测未标注图片的含义。
- 定时提醒当前支持一次性、每天和每周重复，不支持按月、工作日或自定义 Cron 表达式。
- 小火人模式依赖 DeepSeek 对语境的判断，可能错过冷门梗；联网搜索或模型请求失败时保持沉默，不影响原有 `@nao` 模式。

## 功能变更维护要求

每次功能变更至少检查以下内容：

1. 更新本文件中的功能、命令、权限、数据文件和限制。
2. 用户可见命令变化时同步更新 `nao_bot/rules.py` 中的 `@nao 帮助` 文本和 `README.md`。
3. 配置项变化时同步更新 `.env.example`、`compose.yaml` 和部署说明。
4. 添加或更新对应行为测试，并运行 `python -m pytest -q`。
5. Docker 相关变化时运行 `docker compose --env-file .env.example config -q`，必要时重新构建镜像。
